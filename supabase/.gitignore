# Supabase CLI state (linked project ref, branches)
.temp/
.branches/

# Secrets: never commit
.env
.env.*
!.env.example
functions/.env
functions/.env.*
!functions/.env.example

# Test tooling (node-tests/.env.live holds staging keys)
node-tests/node_modules/
node-tests/.env.live
!.env.live.example
